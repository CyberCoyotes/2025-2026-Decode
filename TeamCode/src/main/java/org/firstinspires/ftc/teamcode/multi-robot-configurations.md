# Configuration for Multi-robot Team

```
// In your OpModes, you can even auto-detect which robot:
RobotConfig config = detectRobot(); // Based on hardware or manual selection
MecanumDrive drive = new MecanumDrive(config);
```